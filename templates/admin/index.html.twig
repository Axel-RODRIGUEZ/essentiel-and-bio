{% extends 'base.html.twig' %}
    {% block stylesheets %}
        {{ parent() }}
            <link href="{{ asset('css/admin.css')}}" rel="stylesheet">
    {% endblock %}
        
    {% block main %}

<div class="admin_header">
    <h1>Gestion des produits</h1>
    <a href="{{ path('new_product') }}" class="btn_primary">Créer un produit</a>
</div>

<div class="product_list_container">
    {% for product in products %}
        <div class="product_card">
            
            <div class="product_info">
                <span class="product_category">{{ product.category.name }}</span>
                <h3 class="product_name">{{ product.name }}</h3>
                <span class="product_price">{{ product.price }} €</span>
            </div>

            <div class="product_actions">
                <form method="post" class="form_delete" action="{{ path('delete', {id:product.id}) }}" onsubmit="return confirm('Es-tu sûr de vouloir supprimer ce produit ?');">
                    <input type="submit" value="Supprimer" class="btn_delete btn"/>
                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product.id) }}">
                </form>
                <a href="{{ path('update', {id:product.id}) }}" class="btn_update btn"> Modifier</a>
                {% if product.favorite == false %}
                    <a href="{{ path('app_product_toggle_favorite', {id: product.id}) }}" class="btn_fav btn_fav_add btn">
                        ♡ Ajouter aux favoris
                    </a>
                {% else %}
                    <a href="{{ path('app_product_toggle_favorite', {id: product.id}) }}" class="btn_fav btn_fav_remove btn">
                        ♥ Retirer des favoris
                    </a>
                {% endif %}
            </div>

        </div>
    {% endfor %}
</div>

    {% endblock %}
